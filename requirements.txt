# Dependencias externas. El resto del pipeline usa solo stdlib:
#   normalize.py, bcb_referencial.py, dashboard.py, scripts/watchdog.py,
#   config.py → todos stdlib (sqlite3, json, gzip, urllib, re, pathlib, etc.)
requests>=2.31.0
openpyxl>=3.1.0
python-dotenv>=1.0,<2.0

# Scraper de noticias (ingest_noticias.py + noticias_ingest/, port de boletines).
# scikit-learn carga noticias_ingest/modelo_relevancia.pkl (TF-IDF, entrenado
# con sklearn 1.8.0). El pickle NO garantiza compat entre versiones: tope <2.0
# deliberado — subir de major implica re-picklear el modelo en boletines.
feedparser>=6.0
beautifulsoup4>=4.12
trafilatura>=2.0
rapidfuzz>=3.0
scikit-learn>=1.8,<2.0
cloudscraper>=1.2
curl_cffi>=0.10
googlenewsdecoder>=0.1.7
