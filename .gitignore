# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Distribution
build/
dist/
*.egg

# Env / secretos
.env
.env.*
!.env.example
backup.env
!backup.env.example
secrets.json
credentials.json
*.key
*.pem

# Lockfiles (backup.py, normalize.py)
.backup.*.lock
*.db.lock

# SSH keys defensivo (jamás deberían estar en el repo)
id_ed25519*
id_rsa*
id_ecdsa*
*USERPROFILE*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude local
.claude/

# Logs
logs/
*.log

# Fase 2 derivadas (regenerables desde snapshots/)
p2p_normalized.csv
p2p_normalized.db
p2p_normalized.db-shm
p2p_normalized.db-wal
p2p_metrics.csv

# Snapshots crudos (no trackear en git)
snapshots/
alt/

# Dashboard: el archivo servido por GitHub Pages es index.html (sí trackeado).
# p2p_dashboard.html es un alias local por compatibilidad, no se sube.
p2p_dashboard.html

# Explícitamente NO ignorados (documentación):
# - index.html            → dashboard servido por GitHub Pages
# - bcb_referencial.json  → histórico del referencial, necesario para el dashboard
# - *.py, *.bat, *.md     → código y docs
.tmp_*
